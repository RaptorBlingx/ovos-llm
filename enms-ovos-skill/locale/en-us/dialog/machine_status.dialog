{% if status == 'running' %}{{ machine }} is running{% if power_kw %} at {{ power_kw | voice_unit('kW') }}{% endif %}.{% elif status == 'idle' %}{{ machine }} is idle.{% else %}{{ machine }} is offline.{% endif %}
