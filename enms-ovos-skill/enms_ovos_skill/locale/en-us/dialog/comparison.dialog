{% if machines and machines|length >= 2 %}Machine comparison for today:
{% for m in machines %}
{{ loop.index }}. {{ m.machine_name }}: {{ m.today_stats.energy_kwh|round(1) }} kWh, costing ${{ m.today_stats.cost_usd|round(2) }}
{% endfor %}
{% elif machines and machines|length == 1 %}
{{ machines[0].machine_name }} consumed {{ machines[0].today_stats.energy_kwh|round(1) }} kilowatt-hours today, costing ${{ machines[0].today_stats.cost_usd|round(2) }}. Please specify another machine to compare.
{% else %}
No machines found for comparison.
{% endif %}
