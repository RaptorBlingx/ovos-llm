{% if machines and machines|length >= 2 %}
Comparing {{ machines[0].machine_name }} and {{ machines[1].machine_name }}: {{ machines[0].machine_name }} consumed {{ machines[0].today_stats.energy_kwh|round(1) }} kilowatt-hours today, while {{ machines[1].machine_name }} consumed {{ machines[1].today_stats.energy_kwh|round(1) }} kilowatt-hours.
{% elif machines and machines|length > 0 %}
Machine comparison for today:
{% for m in machines %}
{{ loop.index }}. {{ m.machine_name }}: {{ m.today_stats.energy_kwh|round(1) }} kWh, costing ${{ m.today_stats.cost_usd|round(2) }}
{% endfor %}
{% else %}
No machines found for comparison.
{% endif %}
