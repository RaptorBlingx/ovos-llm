{% if machines is defined %}
{# Machine list response #}
{% if count == 0 %}No machines found matching that criteria.{% else %}We have {{ count }} machines: {% for m in machines %}{{ m.name }}{{ ", " if not loop.last else "." }}{% endfor %}{% endif %}
{% elif ranking is defined %}
{# Top N ranking response #}
Top {{ limit }} {{ metric | lower }} consumers:
{% for machine in ranking %}
{{ machine.rank }}. {{ machine.machine_name }}: {{ machine.value|round(2) }} {{ unit }} ({{ machine.percentage|round(1) }}% of total)
{% endfor %}
{% endif %}
