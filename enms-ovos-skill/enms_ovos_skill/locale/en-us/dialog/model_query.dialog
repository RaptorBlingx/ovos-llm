Model version {{ model_version }} for {{ machine_name }}: Trained using {{ feature_names|length }} feature{% if feature_names|length != 1 %}s{% endif %} ({{ feature_names|join(', ') }}). Model accuracy is {{ (r_squared * 100)|round(1) }}% R-squared with RMSE of {{ rmse|round(2) }} kilowatt-hours. Trained on {{ training_samples }} samples from {{ training_start_date[:10] }} to {{ training_end_date[:10] }}. {% if explanation and explanation.voice_summary %}{{ explanation.voice_summary }}{% endif %}