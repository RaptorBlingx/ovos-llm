Factory status: {{ status }}. Today's energy: {{ energy.total_kwh_today|round(1) }} kilowatt hours at {{ energy.current_power_kw|round(1) }} kilowatts. Cost: ${{ costs.total_usd_today|round(2) }}. Machines: {{ machines.active }} of {{ machines.total }} active{% if anomalies.total_today > 0 %}, {{ anomalies.total_today }} anomal{{ 'y' if anomalies.total_today == 1 else 'ies' }} today{% endif %}. Top consumer: {{ top_consumer.machine_name }} with {{ top_consumer.energy_kwh|round(1) }} kilowatt hours.
